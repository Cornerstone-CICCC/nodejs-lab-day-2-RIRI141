---
import Layout from "../layouts/Layout.astro";
---

<Layout>
  <h1>FANCY Elmer's student's site</h1>
  <button id="btn-login">Log in</button>
  <button id="btn-signup">Sign up</button>
</Layout>

<script>
  const btnlogin = document.querySelector("#btn-login") as HTMLButtonElement;
  const btnsignup = document.querySelector("#btn-signup") as HTMLButtonElement;

  //   const login = async () => {
  //     const res = await fetch(`http://localhost:4000/login`, {
  //       method: "POST",
  //       credentials: "include",
  //     });
  //     if (!res.ok) {
  //       console.log("Something went wrong");
  //       return;
  //     }
  //     const data = await res.json();
  //     return data;
  //   };
  btnlogin?.addEventListener("click", async () => {
    // const data = await login();
    window.location.assign("http://localhost:4321/login");
    // console.log(data);
  });
  btnsignup?.addEventListener("click", async () => {
    window.location.assign("http://localhost:4321/register");
  });
</script>
